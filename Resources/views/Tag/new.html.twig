{% extends '::base.html.twig' %}
{% block body %}

<h1>Tag creation</h1>

<form action="{{ path('tags_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

{% endblock %}